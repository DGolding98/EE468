Start the database from the UniversityApp directory with the following command:
$ python3 manage.py runserver

Connect to the database:
localhost:8000

Connect to the student page:
localhost:8000/student
